org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# opus_flutter_android (pinned at 3.0.1) still declares minSdk 19, and NDK r28 refuses
# to build for anything below 21. The app's own minSdk is 26, so the merged manifest is
# unaffected; this only silences the plugin's stale declaration. See docs/04 §2.
android.ndk.suppressMinSdkVersionError=21
